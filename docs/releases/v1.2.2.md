# Healthcare Lab v1.2.2

## Fixes

- Add automatic fallback to the legacy `docker-compose` command when
  `docker compose` is unavailable.
- Enable the GDT Host Controller and GDT page Apply workflow on existing
  Docker Compose v1 installations.
- Keep Docker Compose v2 as the preferred command when both versions are
  available.
- Allow operators to select and apply a dedicated Windows GDT exchange folder
  without requiring a `.env` override.

## Verification

- Full automated suite: 962 tests passed.
- Environment-dependent skips: 1.
- Release image: `ghcr.io/tzu-huang/healthcare-lab:1.2.2`.
