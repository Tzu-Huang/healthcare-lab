# Healthcare Lab v1.1.1

This patch release preserves bounded Medplum verification state across
application restart and retained-container recreation.

## Fixed

- Persisted allowlisted Medplum verification outcomes against the exact tested
  configuration revision.
- Kept failed required Medplum readiness degraded after restart instead of
  incorrectly returning setup to ready.
- Rejected stale or internally inconsistent verification evidence.
- Preserved secret, authorization, PHI, and FHIR-body redaction boundaries.

## Verification

- The release targets `main` commit
  `54e60d0e69d25c256474d9d0a5c790b1d9b7599e`.
- GitHub Actions run `30242203066` passed product tests, the `linux/amd64`
  image build, anonymous GHCR inspection, and the immutable Settings readiness
  smoke test.
- Isolated retained-container recreation evidence confirms synthetic failed
  Medplum readiness remains degraded and setup remains incomplete.
